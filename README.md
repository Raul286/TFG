# TFG
Sistema de xestión de backups para almacenamento en bloque (LVM/iSCSI) con interface web e API REST
Hoy cree las maquinas, tengo tres maquinas un servidor iscsi que es donde voy a crear el gruo de volumenes y despues los volumenes, esta conectada a la vlan 50,otra maquina que es el backup server, que es donde se van a hacer los backups, esta conectada a la vlan60 y la red comercial, por ultimo una maquina donde voy ahcaer la web que va a controlar todo conectada a la vlan 50
